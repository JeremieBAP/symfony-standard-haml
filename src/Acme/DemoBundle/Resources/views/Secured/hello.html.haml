- extends "AcmeDemoBundle:Secured:layout.html.haml"

- block title "Hello " ~ name

- block content
    %h1 Hello #{ name }!

    %a(href=path('_demo_secured_hello_admin', { 'name': name })) Hello resource secured for <strong>admin</strong> only.

- set code = code(_self)
